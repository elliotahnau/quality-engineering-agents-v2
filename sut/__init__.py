"""The System Under Test (SUT) for Ad operations campaign API.

A FastAPI app with planted bugs.
The ground truth for every planted bug lives in bugs.yaml;
eval harness metrics are computed against it.
"""
